export interface PaidConfig {
  agentId: string;
  paidApiKey: string;
  publishableKey: string;
}

export const paidConfig: PaidConfig = {
  agentId: "blah",
  paidApiKey: "api-key-2",
  publishableKey: "blah",
};
